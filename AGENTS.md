## Agent skills

### Issue tracker

Issue 存放在本仓库的 GitHub Issues（`gh` CLI）；Ticket 使用 `SC-XXX [P?][Area] 描述` 标题格式，并登记到 `docs/tickets.md`。参见 `docs/agents/issue-tracker.md`。

### Triage labels

沿用默认的五个状态标签 `needs-triage` / `needs-info` / `ready-for-agent` / `ready-for-human` / `wontfix`，分类标签沿用 GitHub 默认的 `bug` / `enhancement`。参见 `docs/agents/triage-labels.md`。

### Domain docs

单 context 仓库：`CONTEXT.md` 与 `docs/adr/` 位于仓库根目录（尚未创建，由 `/domain-modeling` 按需生成）；当前领域知识以 `docs/` 下的规格文档为准。参见 `docs/agents/domain.md`。
