{
  "name": "semantic-calendar",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/desktop"
  ],
  "scripts": {
    "dev": "npm run dev --workspace @semantic-calendar/desktop",
    "build": "npm run build --workspace @semantic-calendar/desktop",
    "test": "npm run test --workspace @semantic-calendar/desktop",
    "lint": "npm run lint --workspace @semantic-calendar/desktop",
    "format": "npm run format --workspace @semantic-calendar/desktop",
    "tauri": "npm run tauri --workspace @semantic-calendar/desktop"
  }
}
